<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.mappers.AdminGroupMapper">

	<!--<insert id="createAdminGroup" parameterType="com.spring.model.admin.AdminGroup">
		INSERT INTO `admin_group`(`username`, `password`, `contactMobile`, `contactName`, `createTime`) VALUES
		(#{username}, #{password}, #{contactMobile}, #{contactName}, #{createTime})
	</insert>-->
	
	<select id="getAdminGroups" resultType="map">
		SELECT `groupId`, `groupName` FROM `admin_group` WHERE `status`=1
	</select>

	<!-- <select id="getAdminUserAuthentication" parameterType="com.spring.model.admin.AdminUserAuthentication" resultType="com.spring.model.admin.AdminUser">
		SELECT `username`, `password`, `status` FROM `admin_user` WHERE `username`=#{username} AND `password`=#{password}
	</select> -->


</mapper>